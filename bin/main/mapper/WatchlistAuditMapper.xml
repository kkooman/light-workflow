<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkooman.lightworkflow.watchlist.repository.WatchlistAuditMapper">
    <insert id="save">
        INSERT INTO watchlist_search_audit
            (searched_at, requested_field_count, result_count, request_hash)
        VALUES
            (#{searchedAt}, #{requestedFieldCount}, #{resultCount}, #{requestHash})
    </insert>
</mapper>
